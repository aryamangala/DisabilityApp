# Optional: override backend when running `npx expo start` (e.g. physical device on same network).
# Production/TestFlight builds use EXPO_PUBLIC_BACKEND_URL from eas.json or Expo dashboard.
# Use your Railway *public* HTTPS URL (not *.railway.internal).
EXPO_PUBLIC_BACKEND_URL=https://disabilityapp.up.railway.app
